# EduBox engine

The engine library provides the common `Device` lifecycle, compatibility aliases
for sensors and actuators, the device registry, and handler-based VSCP routing.

Include its public API with `#include <engine.hpp>`.
